coverage:
  status:
    project:
      default:
        target: 80%
        threshold: 1%

comment: # this is a top-level key
  layout: " diff, flags, files"
  behavior: default
  require_changes: false # if true: only post the comment if coverage changes
  require_base: false # [true :: must have a base report to post]
  require_head: true # [true :: must have a head report to post]
  hide_project_coverage: false # [true :: only show coverage on the git diff]

ignore:
  - "edulytica/rag/llm"
  - "edulytica/rag/semantic_search"
  - "edulytica/rag/const.py"
  - "edulytica/rag/extract.py"
  - "edulytica/extracting_rules"
  - "examples"
  - "edulytica/front_end"
  - "edulytica/common/database/crud"
  - "edulytica/common/utils/default_enums.py"
  - "**/app.py"
  - "edulytica/orchestration"